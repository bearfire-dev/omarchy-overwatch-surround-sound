# Agent instructions

- Run `./tests/check.sh` after each change.
- Run `REQUIRE_SHELLCHECK=1 ./tests/check.sh` before publication.
- Do not restart the audio services while Overwatch is open.
- Keep install and uninstall operations reversible.
- Use full commit SHAs for all external GitHub Actions.

There is no automatic fix command.
